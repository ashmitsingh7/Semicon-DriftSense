# Model Weights

This project uses **classical computer vision only** (normalized cross-correlation, phase correlation, parabolic refinement).

No pretrained neural network weights, no deep learning models, and no external model dependencies are required.

All computations run on CPU with OpenCV and NumPy — no GPU, no ML frameworks.